**/target/
*.jar
.idea/
*.iml
node_modules/
.next/
.env
.env.local
*.log
deploy/.env
deploy/Caddyfile

# TypeScript
web/tsconfig.tsbuildinfo

# Ролик-обзор собирается скриптом из текста и артов — держать в репозитории
# семь мегабайт, которые пересобираются одной командой, незачем.
web/update-autumn.mp4

# Локальный стенд: своя база, свой токен, своя сборка — всё машинное.
web/.local-stand/
